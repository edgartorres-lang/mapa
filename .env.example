# Copie para .env e preencha localmente. Nunca commitar o .env de verdade.

# Postgres. Em produção (EasyPanel) isso aponta pro serviço de Postgres do painel.
DATABASE_URL="postgresql://usuario:senha@localhost:5432/mapa_da_protecao?schema=public"
